import { StyledListHeader } from "./styles";
const ListHeader = ({ name }) => {
    return (
        <StyledListHeader>
            {name}
        </StyledListHeader>
    )
}

export default ListHeader;